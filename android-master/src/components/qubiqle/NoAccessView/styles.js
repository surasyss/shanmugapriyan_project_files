import Colors from '../../../styles/Colors';

export default {
  emptyContainer: {
    height: '100%',
    justifyContent: 'center',
  },
  emptyMessage: {
    color: Colors.secondaryText,
    fontSize: 20,
    fontWeight: 'bold',
    width: '100%',
    textAlign: 'center',
  }
};
